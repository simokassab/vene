<div class="py-8">
  <h1 class="text-3xl font-bold text-gray-900 mb-8"><%= @page.title %></h1>
  <div class="prose prose-lg max-w-none prose-headings:text-gray-900 prose-p:text-gray-700 prose-a:text-[#465FFF] prose-strong:text-gray-900" dir="<%= I18n.locale == :ar ? 'rtl' : 'ltr' %>">
    <%= @page.content.html_safe %>
  </div>
</div>
