<!DOCTYPE html>
<html lang="<%= I18n.locale %>" dir="<%= I18n.locale == :ar ? 'rtl' : 'ltr' %>">
  <head>
    <title><%= content_for(:title) || "VENE Jewelry" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js" defer></script>
    <script defer src="https://cdn.jsdelivr.net/npm/@alpinejs/intersect@3.x.x/dist/cdn.min.js"></script>
  </head>

  <body class="bg-white text-gray-900" x-data="{ mobileMenu: false, searchOpen: false }">
    <header class="fixed top-0 inset-x-0 backdrop-blur z-30 shadow-lg" style="background-color: #131313;">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-20 gap-8">
          <!-- Left icons -->
          <div class="flex items-center gap-4 flex-1">
            <button class="md:hidden text-white" @click="mobileMenu = !mobileMenu">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
              </svg>
            </button>
            <% if user_signed_in? %>
              <%= link_to orders_path(locale: I18n.locale), class: "hidden md:block text-white hover:text-gray-300 transition-colors" do %>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
              <% end %>
            <% else %>
              <%= link_to new_user_session_path(locale: I18n.locale), class: "hidden md:block text-white hover:text-gray-300 transition-colors" do %>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
              <% end %>
            <% end %>
          </div>

          <!-- Center logo -->
          <div class="absolute left-1/2 transform -translate-x-1/2">
            <%= link_to root_path(locale: I18n.locale), class: "block" do %>
              <%= image_tag "logo.jpg", alt: "Logo", class: "h-12 md:h-16 w-auto object-contain" %>
            <% end %>
          </div>

          <!-- Right icons -->
          <div class="flex items-center gap-4 flex-1 justify-end">
            <button @click="searchOpen = !searchOpen" class="text-white hover:text-gray-300 transition-colors">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
              </svg>
            </button>
            <%= link_to cart_path(locale: I18n.locale), class: "relative text-white hover:text-gray-300 transition-colors" do %>
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
              </svg>
              <% if cart_item_count.positive? %>
                <span class="absolute -top-2 -right-3 min-w-[1.5rem] px-2 py-0.5 rounded-full bg-red-600 text-white text-xs font-semibold text-center shadow-sm">
                  <%= cart_item_count %>
                </span>
              <% end %>
            <% end %>
            <div class="hidden md:flex items-center text-sm gap-1">
              <%= link_to "EN", url_for(locale: :en), class: "#{I18n.locale == :en ? 'font-semibold text-white' : 'text-gray-400 hover:text-gray-300'} transition-colors" %>
              <span class="text-gray-500">/</span>
              <%= link_to "AR", url_for(locale: :ar), class: "#{I18n.locale == :ar ? 'font-semibold text-white' : 'text-gray-400 hover:text-gray-300'} transition-colors" %>
            </div>
          </div>
        </div>

        <!-- Categories Navigation -->
        <div class="hidden md:flex items-center justify-center gap-8 pb-4 border-b border-gray-700">
          <% Category.active.ordered.each do |category| %>
            <%= link_to category.name(I18n.locale), category_path(category.slug, locale: I18n.locale), class: "text-sm font-medium text-white hover:text-gray-300 transition-colors" %>
          <% end %>
        </div>

        <!-- Animated Search Bar -->
        <div x-show="searchOpen"
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 transform -translate-y-4"
             x-transition:enter-end="opacity-100 transform translate-y-0"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 transform translate-y-0"
             x-transition:leave-end="opacity-0 transform -translate-y-4"
             class="py-4 border-t border-gray-700">
          <form action="<%= search_path(locale: I18n.locale) %>" method="get" class="max-w-2xl mx-auto">
            <div class="flex items-center border-2 border-gray-600 rounded-full px-4 py-2 bg-gray-800">
              <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
              </svg>
              <input type="text"
                     name="q"
                     placeholder="Search jewelry..."
                     value="<%= params[:q] %>"
                     class="w-full focus:outline-none text-sm bg-gray-800 text-white placeholder-gray-400"
                     x-ref="searchInput"
                     @keydown.escape="searchOpen = false">
              <button type="button" @click="searchOpen = false" class="ml-2 text-gray-400 hover:text-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="md:hidden" x-show="mobileMenu" x-transition>
        <div class="px-4 pb-4 space-y-2 border-t border-gray-700" style="background-color: #131313;">
          <% Category.active.ordered.each do |category| %>
            <%= link_to category.name(I18n.locale), category_path(category.slug, locale: I18n.locale), class: "block py-2 text-sm text-white hover:text-gray-300" %>
          <% end %>
          <div class="flex items-center text-sm gap-2 pt-2 border-t border-gray-700 text-white">
            <% if user_signed_in? %>
              <%= link_to t("nav.my_account"), orders_path(locale: I18n.locale), class: "text-sm hover:text-gray-300" %>
            <% else %>
              <%= link_to t("nav.login"), new_user_session_path(locale: I18n.locale), class: "text-sm hover:text-gray-300" %>
            <% end %>
            <%= link_to cart_path(locale: I18n.locale), class: "text-sm relative hover:text-gray-300" do %>
              <span><%= t("nav.cart") %></span>
              <% if cart_item_count.positive? %>
                <span class="ml-2 inline-flex items-center justify-center min-w-[1.5rem] px-2 py-0.5 rounded-full bg-red-600 text-white text-xs font-semibold">
                  <%= cart_item_count %>
                </span>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <main class="pt-20 min-h-screen">
      <% if content_for?(:full_width) %>
        <%= yield %>
      <% else %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <% flash.each do |key, value| %>
            <div class="mb-4 rounded border px-4 py-3 <%= key.to_s == 'alert' ? 'border-red-200 bg-red-50 text-red-700' : 'border-emerald-200 bg-emerald-50 text-emerald-700' %>">
              <%= value %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      <% end %>
    </main>

    <footer class="bg-black text-gray-200 mt-10">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 grid grid-cols-1 md:grid-cols-3 gap-6">
        <div>
          <div class="font-semibold text-lg mb-2"><%= current_settings.store_name %></div>
          <p class="text-sm text-gray-400"><%= t("brand.tagline") %></p>
        </div>
        <div>
          <div class="font-semibold mb-2"><%= t("footer.legal") %></div>
          <ul class="space-y-1 text-sm">
            <li><%= link_to t("pages.terms"), page_path("terms-and-conditions", locale: I18n.locale), class: "hover:text-white" %></li>
            <li><%= link_to t("pages.privacy"), page_path("privacy-policy", locale: I18n.locale), class: "hover:text-white" %></li>
            <li><%= link_to t("pages.delivery"), page_path("delivery-and-return-policy", locale: I18n.locale), class: "hover:text-white" %></li>
          </ul>
        </div>
        <div>
          <div class="font-semibold mb-2"><%= t("footer.contact") %></div>
          <p class="text-sm text-gray-400"><%= t("footer.whatsapp") %>: <%= current_settings.whatsapp_phone_number.presence || "TBD" %></p>
        </div>
      </div>
    </footer>

    <% if current_settings.whatsapp_phone_number.present? %>
      <a href="https://wa.me/<%= current_settings.whatsapp_phone_number %>" target="_blank" class="fixed bottom-5 right-5 bg-gray-700 hover:bg-gray-800 text-white rounded-full px-4 py-3 shadow-lg flex items-center gap-2">
        <span>WhatsApp</span>
      </a>
    <% end %>
  </body>
</html>
