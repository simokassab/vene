<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900"><%= t("admin.coupons.edit") %></h1>
  <p class="mt-1 text-sm text-gray-600">
    <%= t("admin.coupons.editing_coupon", default: "Editing coupon:") %>
    <span class="font-mono font-bold"><%= @coupon.code %></span>
  </p>
</div>

<%= render "form", coupon: @coupon %>
