<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900"><%= t("admin.coupons.new") %></h1>
  <p class="mt-1 text-sm text-gray-600"><%= t("admin.coupons.new_subtitle", default: "Create a new discount coupon") %></p>
</div>

<%= render "form", coupon: @coupon %>
