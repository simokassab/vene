<div class="mb-6">
  <div class="flex items-center space-x-3">
    <%= link_to admin_sub_categories_path(locale: I18n.locale), class: "text-gray-600 hover:text-gray-900 transition-colors" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
    <% end %>
    <h1 class="text-2xl font-bold text-gray-900">
      <%= t("admin.sub_categories.edit_title", default: "Edit Subcategory") %>
    </h1>
  </div>
  <p class="mt-2 text-sm text-gray-600"><%= @sub_category.name_en %></p>
</div>

<%= render "form" %>
